@{
    var path = Context.Request.Path.Value;
    if (User.Identity.IsAuthenticated && path != "/Account/Login")
    {
        Layout = "_Layout";
    }
    else
    {
        Layout = "_LayoutGuest";
    }
}
